// Editor configuration options
export const config = {
    // Editor theme - 'vs' (light), 'vs-dark' (dark), or 'hc-black' (high contrast)
    theme: 'vs-dark',
    
    // Font size in pixels
    fontSize: 14,
    
    // Tab size in spaces
    tabSize: 2,
    
    // Whether to automatically save on changes
    autoSave: false,
    
    // Default template to load on startup (empty for none)
    defaultTemplate: '',
    
    // Enable word wrap
    wordWrap: 'on',
    
    // Enable minimap
    minimap: true,
    
    // Enable line numbers
    lineNumbers: true
};

